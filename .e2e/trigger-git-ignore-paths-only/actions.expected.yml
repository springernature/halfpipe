# Generated using halfpipe cli version 0.0.0-DEV from file .e2e/trigger-git-ignore-paths-only/.halfpipe.io.yml for team halfpipe-team
name: pipeline-name
"on":
  push:
    branches:
    - main
    paths:
    - '**'
    - '!README.md**'
    - '!**.js**'
  workflow_dispatch: {}
permissions:
  contents: read
  actions: read
  security-events: write
env:
  ARTIFACTORY_PASSWORD: ${{ secrets.EE_ARTIFACTORY_PASSWORD }}
  ARTIFACTORY_URL: ${{ secrets.EE_ARTIFACTORY_URL }}
  ARTIFACTORY_USERNAME: ${{ secrets.EE_ARTIFACTORY_USERNAME }}
  BUILD_VERSION: 2.${{ github.run_number }}.0
  GIT_REVISION: ${{ github.sha }}
  RUNNING_IN_CI: "true"
defaults:
  run:
    working-directory: .e2e/trigger-git-ignore-paths-only
concurrency: ${{ github.workflow }}
jobs:
  run__echo_hello:
    name: run \echo hello
    runs-on: ee-runner
    timeout-minutes: 60
    steps:
    - name: Checkout code
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        lfs: true
        show-progress: false
        ssh-key: ${{ secrets.EE_GITHUB_PRIVATE_KEY }}
        submodules: recursive
    - name: run \echo hello
      id: run-echo-hello
      uses: docker://ubuntu
      with:
        args: -c "cd .e2e/trigger-git-ignore-paths-only; \echo hello"
        entrypoint: /bin/sh
      env:
        CI: "true"
        EAID: APPL-437
        EE_PLATFORM_TEAM: halfpipe-team
        RUNNING_IN_CI: "true"
